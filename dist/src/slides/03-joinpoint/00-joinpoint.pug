section
    h2 Join Point
    p A point during the execution of a program, such as the execution of a method or the handling of an exception. In Spring AOP, a join point always represents a method execution.

section
    h2 So What's a Join Point?
    p Its the part of your code that the pointcut matches
